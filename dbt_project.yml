# name: "dbt_learning"
name: "nathan_dbt_project"
version: "1.0.0"
config-version: 2

profile: "my_new_project"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"

clean-targets:
  - "target"
  - "dbt_packages"
  - "logs"

models:
  nathan_dbt_workshop:
    +database: PC_DBT_DB
    silver:
        +schema: silver
        +materialized: table
    gold:
        +schema: gold
        +materialized: view